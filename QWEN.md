## Qwen Added Memories
- ClamGUI session state - Apr 4:
- ScanResultsDatabase.swift now compiles (was missing from PBXSourcesBuildPhase)
- Fixed double enqueue: DirectoryWatcher no longer calls scanFileBackground (only WatchdogView does)
- Added extensive logging to trace completion handler chain
- DB write SUCCESS: recordScanSync logs "Successfully recorded/updated" 
- DB read FAILS: getInfectedFiles returns 0 rows despite write success
- Added SELECT * dump to getInfectedFiles to debug why
- Next: User will provide flowchart tomorrow to define desired scan flow
- Current blocker: Found Threats list empty, need to understand why query returns empty
- File paths: ~/Library/Application Support/ClamGUI/scan_results.db
- ClamGUI session state - Apr 4:
- Implemented universal Command Queue with Priorities (Highest/High/Normal/Low)
- QueueCommand.swift defines ScanCommand, ControlCommand (Reload, Stats, Version, Shutdown, Ping)
- QueueManager.swift now processes generic commands synchronously from a loop
- Fixed build errors: added missing methods (removeFile, suspendQueue, resumeQueue, scanStatus) to QueueManager
- ClamAVManager and DirectoryWatcher updated to use new API
- Next: User provided flowchart (Mermaid) - will implement startup sequence (Daemon Gate -> Watchdog & Queue start)
- ClamGUI session state - Apr 4:
- Implemented Daemon Gate Flowchart logic.
- Junction n12 implemented: Daemon Ready -> QueueManager.start() + Watchdog auto-start.
- QueueManager no longer starts in init(); requires explicit start().
- WatchdogView automatically toggles 'Active' when Daemon becomes ready and directory is set.
- ClamAVManager.startClamd and checkClamAVInstallation trigger the junction logic.
- StopClamd suspends the queue.
- Build succeeded.
